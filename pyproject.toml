[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "AEnvironment"
version = "0.0.1"
description = "xxx"
requires-python = ">=3.8"
dependencies = [
    "torch",
    "pygame"
]